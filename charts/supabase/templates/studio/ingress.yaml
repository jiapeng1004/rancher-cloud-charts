{{- if and .Values.deployment.studio.enabled .Values.ingress.studio.enabled }}
{{- $sh := (.Values.ingress.studio.hostname | default "") | trim }}
{{- if eq $sh "" }}
{{- fail "[supabase] ingress.studio.enabled=true 时须设置 ingress.studio.hostname" }}
{{- end }}
{{- $fullName := include "supabase.studio.fullname" . -}}
{{- $ingName := printf "%s-ingress" $fullName | trunc 63 | trimSuffix "-" -}}
{{- $svcPort := .Values.service.studio.port -}}
{{- $kv := default .Capabilities.KubeVersion.Version .Capabilities.KubeVersion.GitVersion }}
{{- $ingressV1 := semverCompare ">=1.19-0" $kv }}
{{- $className := .Values.ingress.studio.className | default .Values.ingress.className }}
{{- $path := .Values.ingress.studio.path | default "/" -}}
{{- $pathType := .Values.ingress.studio.pathType | default "Prefix" -}}
{{- $defTls := (.Values.ingress.tlsSecretName | default "") | trim -}}
{{- $studioTls := (.Values.ingress.studio.tlsSecretName | default "") | trim -}}
{{- $tlsSecret := ternary $studioTls $defTls (ne $studioTls "") }}
{{- if $ingressV1 }}
apiVersion: networking.k8s.io/v1
{{- else }}
apiVersion: networking.k8s.io/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $ingName }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
    app.kubernetes.io/component: studio
  {{- with .Values.ingress.studio.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and $className (semverCompare ">=1.18-0" $kv) }}
  ingressClassName: {{ $className }}
  {{- end }}
  {{- if ne $tlsSecret "" }}
  tls:
    - hosts:
        - {{ $sh | quote }}
      secretName: {{ $tlsSecret }}
  {{- end }}
  rules:
    - host: {{ $sh | quote }}
      http:
        paths:
          {{- if $ingressV1 }}
          - path: {{ $path }}
            pathType: {{ $pathType }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
          {{- else }}
          - path: {{ $path }}
            backend:
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
          {{- end }}
{{- end }}
