categories:
  - supabase
  - postgres
  - database

questions:
  - variable: ingress.enabled
    type: boolean
    default: true
    label: 启用 Ingress
    description: Kong 网关对外的 HTTP Ingress；关闭时需自行暴露 Service。
    group: Ingress

  - variable: ingress.className
    type: string
    default: "nginx"
    label: IngressClass
    description: Ingress 控制器类名。
    group: Ingress

  - variable: ingress.hostname
    type: string
    default: "supabase.local"
    label: 统一对外域名
    description: >-
      单域名部署入口。Auth 外部 URL、GoTrue 站点 URL、Studio 公共 URL
      均以此值自动推导（http://<域名>），无需额外填写。
    group: Ingress
    required: true
    show_if: ingress.enabled=true

  - variable: global.storageClassName
    type: string
    default: ""
    label: 全局 PVC StorageClass（必填）
    description: >-
      所有需要持久卷的组件共用此 StorageClass。
      必须填写集群中存在的 StorageClass 名称，如 local-path、nfs-client 等。
    group: 持久卷
    required: true

  - variable: secret.jwt.secret
    type: password
    default: "your-super-secret-jwt-token-with-at-least-32-characters-long"
    label: JWT 签名密钥（≥32 字符）
    description: >-
      Supabase / PostgREST / Realtime 等共用的签名密钥；请务必在生产改为随机串（openssl rand -hex 32）。
    min_length: 32
    required: true
    group: 密钥与安全

  - variable: deployment.db.enabled
    type: boolean
    default: true
    label: 部署内置 Postgres（StatefulSet）
    description: >-
      「关」时使用外链库，需填写主机与端口。**关**时必须确保库已就绪并完成扩展/迁移。
    group: Postgres

  - variable: secret.db.host
    type: string
    default: ""
    label: 外链 Postgres 主机名
    description: 仅在 **关闭内置库**（deployment.db.enabled=false）时必填；不写协议前缀。
    group: Postgres

  - variable: secret.db.port
    type: string
    default: "5432"
    label: 外链 Postgres 端口
    group: Postgres

  - variable: secret.db.password
    type: password
    default: "your-super-secret-and-long-postgres-password"
    label: Postgres 密码
    description: >-
      内置库或外链库的 postgres 口令；表单安装后请务必更换。
    required: true
    group: Postgres

  - variable: secret.db.database
    type: string
    default: "postgres"
    label: 数据库库名
    group: Postgres

  - variable: secret.realtime.secretKeyBase
    type: password
    default: "UpNVntn3cDxHJpq99YMc1T1AQgQpc8kfYTuRgBiYa15BLrx8etQoXz3gZv1/u2oq"
    label: Realtime SECRET_KEY_BASE（≥43 字符）
    description: >-
      Phoenix 等价机密；生成方式：openssl rand -base64 64。**不少于 43 字符**可避免常见自检失败。
    min_length: 43
    required: true
    group: 密钥与安全

  - variable: secret.meta.cryptoKey
    type: password
    default: "your-encryption-key-32-chars-min"
    label: Meta 加密密钥（≥32 字符）
    description: >-
      用于加密敏感元数据；生成方式：openssl rand -hex 32
    min_length: 32
    group: 密钥与安全

  - variable: secret.analytics.publicAccessToken
    type: password
    default: "your-super-secret-and-long-logflare-key-public"
    label: Analytics Public Access Token
    description: Logflare 公钥
    group: 密钥与安全

  - variable: secret.analytics.privateAccessToken
    type: password
    default: "your-super-secret-and-long-logflare-key-private"
    label: Analytics Private Access Token
    description: Logflare 私钥
    group: 密钥与安全

  - variable: secret.smtp.username
    type: string
    default: "fake_mail_user"
    label: SMTP 用户名
    group: 密钥与安全

  - variable: secret.smtp.password
    type: password
    default: "fake_mail_password"
    label: SMTP 密码
    group: 密钥与安全

  - variable: secret.dashboard.username
    type: string
    default: "supabase"
    label: Studio 用户名
    group: 密钥与安全

  - variable: secret.dashboard.password
    type: password
    default: "this_password_is_insecure_and_should_be_updated"
    label: Studio 密码
    group: 密钥与安全

  - variable: secret.s3.keyId
    type: string
    default: "625729a08b95bf1b7ff351a663f3a23c"
    label: S3 Access Key ID
    description: S3 兼容存储的 Access Key
    group: 密钥与安全

  - variable: secret.s3.accessKey
    type: password
    default: "850181e4652dd023b7a98c58ae0d2d34bd487ee0cc3254aed6eda37307425907"
    label: S3 Secret Access Key
    description: S3 兼容存储的 Secret Key
    group: 密钥与安全

  - variable: secret.minio.user
    type: string
    default: "supa-storage"
    label: MinIO 用户名
    group: 密钥与安全

  - variable: secret.minio.password
    type: password
    default: "secret1234"
    label: MinIO 密码
    group: 密钥与安全

  - variable: deployment.studio.enabled
    type: boolean
    default: true
    label: 部署 Studio 管理控制台
    group: 组件开关

  - variable: deployment.minio.enabled
    type: boolean
    default: false
    label: 部署内置 MinIO（对象存储）
    description: >-
      无对象存储时请改 **true** 或接云厂商 S3 并在 YAML 配置。
    group: 组件开关

  - variable: deployment.analytics.enabled
    type: boolean
    default: true
    label: 部署 Logflare / Analytics（分析）
    group: 组件开关

  - variable: deployment.vector.enabled
    type: boolean
    default: true
    label: 部署 Vector（日志管线）
    group: 组件开关

  - variable: deployment.rest.enabled
    type: boolean
    default: true
    label: 部署 PostgREST（REST API）
    group: 组件开关

  - variable: deployment.meta.enabled
    type: boolean
    default: true
    label: 部署 Postgres Meta（数据库管理）
    group: 组件开关

  - variable: deployment.imgproxy.enabled
    type: boolean
    default: true
    label: 部署 imgproxy（图片处理）
    group: 组件开关

  - variable: deployment.functions.enabled
    type: boolean
    default: true
    label: 部署 Edge Functions（边缘函数）
    group: 组件开关

  - variable: deployment.auth.enabled
    type: boolean
    default: true
    label: 部署 GoTrue（认证服务）
    group: 组件开关

  - variable: deployment.kong.enabled
    type: boolean
    default: true
    label: 部署 Kong（API 网关）
    group: 组件开关

  - variable: deployment.realtime.enabled
    type: boolean
    default: true
    label: 部署 Realtime（WebSocket）
    group: 组件开关

  - variable: deployment.storage.enabled
    type: boolean
    default: true
    label: 部署 Storage API（对象存储服务）
    group: 组件开关

  - variable: image.db.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/supabase/postgres"
    label: 数据库镜像仓库
    group: 镜像（国内前缀）

  - variable: image.db.tag
    type: string
    default: "15.8.1.085"
    label: 数据库镜像标签
    group: 镜像（国内前缀）

  - variable: image.kong.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/kong/kong"
    label: Kong 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.kong.tag
    type: string
    default: "3.9.1"
    label: Kong 镜像标签
    group: 镜像（国内前缀）

  - variable: image.minio.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/minio/minio"
    label: MinIO 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.minio.tag
    type: string
    default: "latest"
    label: MinIO 镜像标签
    group: 镜像（国内前缀）

  - variable: image.auth.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/supabase/gotrue"
    label: Auth / GoTrue 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.auth.tag
    type: string
    default: "v2.186.0"
    label: Auth 镜像标签
    group: 镜像（国内前缀）

  - variable: image.studio.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/supabase/studio"
    label: Studio 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.studio.tag
    type: string
    default: "2026.04.27-sha-5f60601"
    label: Studio 镜像标签
    group: 镜像（国内前缀）

  - variable: image.rest.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/postgrest/postgrest"
    label: PostgREST 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.rest.tag
    type: string
    default: "v14.8"
    label: PostgREST 镜像标签
    group: 镜像（国内前缀）

  - variable: image.realtime.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/supabase/realtime"
    label: Realtime 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.realtime.tag
    type: string
    default: "v2.76.5"
    label: Realtime 镜像标签
    group: 镜像（国内前缀）

  - variable: image.storage.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/supabase/storage-api"
    label: Storage API 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.storage.tag
    type: string
    default: "v1.48.26"
    label: Storage API 镜像标签
    group: 镜像（国内前缀）

  - variable: image.meta.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/supabase/postgres-meta"
    label: Postgres Meta 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.meta.tag
    type: string
    default: "v0.96.3"
    label: Postgres Meta 镜像标签
    group: 镜像（国内前缀）

  - variable: image.functions.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/supabase/edge-runtime"
    label: Edge Functions 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.functions.tag
    type: string
    default: "v1.71.2"
    label: Edge Functions 镜像标签
    group: 镜像（国内前缀）

  - variable: image.analytics.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/supabase/logflare"
    label: Analytics / Logflare 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.analytics.tag
    type: string
    default: "1.36.1"
    label: Analytics 镜像标签
    group: 镜像（国内前缀）

  - variable: image.vector.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/timberio/vector"
    label: Vector 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.vector.tag
    type: string
    default: "0.53.0-alpine"
    label: Vector 镜像标签
    group: 镜像（国内前缀）

  - variable: image.imgproxy.repository
    type: string
    default: "swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/darthsim/imgproxy"
    label: imgproxy 镜像仓库
    group: 镜像（国内前缀）

  - variable: image.imgproxy.tag
    type: string
    default: "v3.30.1"
    label: imgproxy 镜像标签
    group: 镜像（国内前缀）