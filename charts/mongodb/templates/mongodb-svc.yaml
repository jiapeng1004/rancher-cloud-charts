apiVersion: v1
kind: Service
metadata:
  name: {{.Chart.Name}}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{.Chart.Name}}
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
      nodePort: 30017
      name: mongo-27017
  type: NodePort
  