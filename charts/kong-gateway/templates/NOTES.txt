========================================
 kong-gateway（声明式）
========================================
{{- $name := default .Chart.Name .Values.nameOverride | trunc 63 | trimSuffix "-" }}
{{- $fullname := .Values.fullnameOverride | default (printf "%s-%s" .Release.Name $name) | trunc 63 | trimSuffix "-" }}

Proxy：Service `{{ printf "%s-proxy" $fullname }}` 端口 {{ .Values.service.proxy.port }} → Pod 8000。
{{- if .Values.service.admin.enabled }}

Admin API：Service `{{ printf "%s-admin" $fullname }}` 端口 {{ .Values.service.admin.port }}（仅限集群内需自行保护）。
{{- end }}

Declarative：ConfigMap `{{ $fullname }}-declarative`。路由请修改 `values.yaml` 里的 `declarative.config`。
