apiVersion: v1
kind: Service
metadata:
  name: {{.Chart.Name}}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{.Chart.Name}}
  ports:
    - protocol: TCP
      port: {{.Values.port}}
      targetPort: {{.Values.port}}
      nodePort: 30379
  type: NodePort
  