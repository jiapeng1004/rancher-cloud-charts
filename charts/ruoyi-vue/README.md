若依（RuoYi）生态镜像众多（Vue2/Vue3/Cloud 前后端拆分等）。本 Chart 仅占位 **单容器**：默认镜像为 nginx，请将 `values.image` 替换为社区构建的 ``ruoyi-admin`` / ``ruoyi-ui`` Docker 镜像，并按需增加第二个 Deployment（前后端拆分）。

可参考社区镜像前先阅读许可证与 Dockerfile 安全性。
