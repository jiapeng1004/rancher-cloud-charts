apiVersion: apps/v1
kind: Deployment
metadata:
  name: snowy-xxl-job-app
  labels:
    app: snowy-xxl-job-app
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snowy-xxl-job-app
  template:
    metadata:
      name: snowy-xxl-job-app
      labels:
        app: snowy-xxl-job-app
    spec:
      initContainers:
        - name: ping-nacos
          image: swr.cn-north-4.myhuaweicloud.com/ddn-k8s/gcr.io/google-containers/busybox:latest
          command: ["/bin/sh"]
          args:
             - "-c"
             - "until wget '{{.Values.image.nacos.repository}}:8848/nacos/'; do echo 'wait nacos'; sleep 1; done"
          imagePullPolicy: IfNotPresent
      containers:
        - name: snowy-xxl-job-app
          image: {{.Values.baseRepo}}{{.Values.image.xxljob.repository}}:{{.Values.image.xxljob.tag}}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9004
              protocol: TCP
          env:
            - name: NACOS_SERVER
              value: "{{.Values.image.nacos.repository}}:8848"
      dnsPolicy: ClusterFirst
      terminationGracePeriodSeconds: 30
      restartPolicy: Always
      