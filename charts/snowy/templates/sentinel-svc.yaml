apiVersion: v1
kind: Service
metadata:
  name: snowy-sentinel-app
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: snowy-sentinel-app
  ports:
    - protocol: TCP
      name: sentinel-9002
      port: 9002
      targetPort: 9002
  type: ClusterIP
  