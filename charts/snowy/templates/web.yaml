apiVersion: apps/v1
kind: Deployment
metadata:
  name: snowy-web-app
  labels:
    app: snowy-web-app
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snowy-web-app
  template:
    metadata:
      name: snowy-web-app
      labels:
        app: snowy-web-app
    spec:
      initContainers:
        - name: ping-nacos
          image: swr.cn-north-4.myhuaweicloud.com/ddn-k8s/gcr.io/google-containers/busybox:latest
          command: ["/bin/sh"]
          args:
             - "-c"
             - "until wget '{{.Values.image.nacos.repository}}:8848/nacos/'; do echo 'wait nacos'; sleep 1; done"
          imagePullPolicy: IfNotPresent
      containers:
        - name: snowy-web-app
          image: {{.Values.baseRepo}}{{.Values.image.web.repository}}:{{.Values.image.web.tag}}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9101
              protocol: TCP
          env:
            - name: NACOS_SERVER
              value: "{{.Values.image.nacos.repository}}:8848"
      imagePullSecrets:
        - name: my-registry-secret
      dnsPolicy: ClusterFirst
      terminationGracePeriodSeconds: 30
      restartPolicy: Always
      