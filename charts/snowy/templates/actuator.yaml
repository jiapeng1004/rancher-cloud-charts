apiVersion: apps/v1
kind: Deployment
metadata:
  name: snowy-actuator-app
  labels:
    app: snowy-actuator-app
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snowy-actuator-app
  template:
    metadata:
      name: snowy-actuator-app
      labels:
        app: snowy-actuator-app
    spec:
      initContainers:
        - name: ping-nacos
          image: swr.cn-north-4.myhuaweicloud.com/ddn-k8s/registry.k8s.io/e2e-test-images/busybox:1.29-4
          command: ["/bin/sh"]
          args:
             - "-c"
             - "until wget '{{.Values.image.nacos.repository}}.{{.Release.Namespace}}:8848/nacos/'; do echo 'wait nacos'; sleep 1; done"
          imagePullPolicy: IfNotPresent
      containers:
        - name: snowy-actuator-app
          image: {{.Values.baseRepo}}{{.Values.image.actuator.repository}}:{{.Values.image.actuator.tag}}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9001
              protocol: TCP
          env:
            - name: NACOS_SERVER
              value: "{{.Values.image.nacos.repository}}.{{.Release.Namespace}}:8848"
      dnsPolicy: ClusterFirst
      terminationGracePeriodSeconds: 30
      restartPolicy: Always
      