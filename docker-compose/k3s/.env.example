# 复制为 .env 后按需覆盖

# 镜像加速（compose 内两处共用，无需改 Docker daemon.json）
DOCKER_MIRROR=docker.1ms.run

K3S_TOKEN=local-dev-token
K3S_VERSION=v1.31.5-k3s1
K3S_API_PORT=6443
K3S_HTTP_PORT=8088
K3S_HTTPS_PORT=8443
